// src/config.ts
import { quansync } from "quansync/macro";
import { loadConfig as _loadConfig } from "unconfig";
var loadConfig = quansync(
  function* (cwd) {
    const { config } = yield _loadConfig({
      sources: [
        {
          files: "vue-macros.config",
          extensions: ["mts", "cts", "ts", "mjs", "cjs", "js", "json", ""]
        },
        {
          files: "package.json",
          extensions: [],
          rewrite: (config2) => config2?.vueMacros
        }
      ],
      defaults: {},
      cwd
    });
    return config;
  }
);

export {
  loadConfig
};
